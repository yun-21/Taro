// src/App.tsx
import React from 'react';
import styles from './styles'; // styles.ts 파일을 import

const App: React.FC = () => {
  return (
    <div style={styles}>
      타로 프로젝트
    </div>
  );
};

export default App;
